/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'fontawesome.com', "picsum.photos"],
      },
}

module.exports = nextConfig
